import { Mulish } from "next/font/google";
import { Poppins } from "next/font/google";

export const poppins = Poppins({ weight: ["400", "700"], subsets: ["devanagari"] });
export const mulish = Mulish({ weight: ["900"], subsets: ["cyrillic"] });